pre-commit:
  commands:
    biome:
      glob: "*.{js,ts,mts,cts,jsx,tsx,svelte,css,json,yaml,yml,toml,html}"
      run: bunx biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true
